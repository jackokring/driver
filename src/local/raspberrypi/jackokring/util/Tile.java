package local.raspberrypi.jackokring.util;

import local.raspberrypi.jackokring.driver.*;

public class Tile extends Model {
	
	public static float txs = 0, tys = 0, txc = 1, tyc = 1;

	public float getZ(Model m) {
		//do generate fill here of caches
		return 0;
	}
}
